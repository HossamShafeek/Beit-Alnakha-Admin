enum MenuActionEnum {
  viewDetails,
  edit,
  delete,
  updateStatus,
  addNote,
  whatsappContact,
  addFeature,
}

enum RequestStatusEnum {
  newText,
  preparing,
  prepared,
  delivering,
  delivered,
  cancelled,
}